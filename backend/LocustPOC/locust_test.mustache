from locust import HttpUser, task, between

class {{class_name}}(HttpUser):
    wait_time = between(1, 3)
{{#test_cases}}
    @task
    def {{method_name}}(self):
        {{#query_param_variables}}
        # Dynamic query parameters
        {{query_param_variables}}
        {{/query_param_variables}}
        {{#path_variable_assignments}}
        # Dynamic path variables
        {{path_variable_assignments}}
        {{/path_variable_assignments}}
        {{#has_payload}}
        
        # Handle JSON payload
        payload = {{payload}}

        # Send JSON payload
        self.client.{{http_method}}(f"{{path_with_params}}", json=payload)

        {{/has_payload}}
        {{^has_payload}}
        self.client.{{http_method}}(f"{{path_with_params}}")
        {{/has_payload}}
{{/test_cases}}


